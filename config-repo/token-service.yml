spring:
  application:
    name: token-service

bank-iot-platform:

  redis:
    enabled: true

  kafka:
    enabled: true
    consumer:
      group-id: token-service

  token:
    access-token-ttl: 300
    refresh-token-ttl: 86400

  session:
    enabled: true
    ttl: 86400